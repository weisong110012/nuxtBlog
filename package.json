{
  "name": "nuxtBlog",
  "version": "1.0.0",
  "description": "nuxtBlog",
  "author": "weisong",
  "private": true,
  "config": {
    "nuxt": {
      "host": "localhost",
      "port": "80"
    }
  },
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build && npm start",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.3.6",
    "@nuxtjs/proxy": "^1.3.1",
    "cross-env": "^5.2.0",
    "koa": "^2.5.2",
    "less": "^3.9.0",
    "less-loader": "^4.1.0",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  }
}
